// This is a placeholder for the API route
// In production, this will be handled by Cloudflare Functions
// For now, we'll use static images and implement dynamic generation later

export default function handler(req, res) {
  // This will be implemented as a Cloudflare Function
  res.status(200).json({ message: 'OG image generation endpoint' });
} 